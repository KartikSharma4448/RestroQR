/**
 * Jest test setup.
 * Import this in test files that need database or app setup.
 *
 * The default test timeout is configured in jest.config.ts (30s)
 * to accommodate property-based tests with 100+ iterations.
 */

export {};
